---
title: 其他库
description: Vue3 Mobile Table 支持所有桌面端 Vue3 UI 组件库中的 Table 组件。
icon: lucide:table-2
---

已内置预设的组件库请参阅 [API](/zh/api/parameters#tablepreset)，使用 `preset` 即可快速接入。

### Arco Design Vue

```vue
<template>
  <div v-mobile-table="{ preset: 'arco-design' }">
    <a-table :columns="columns" :data="data" :scroll="{ x: 1200 }" />
  </div>
</template>
```

### Vuetify

```vue
<template>
  <div v-mobile-table="{ preset: 'vuetify' }">
    <v-data-table :headers="headers" :items="items" />
  </div>
</template>
```

### PrimeVue

```vue
<template>
  <div v-mobile-table="{ preset: 'primevue' }">
    <DataTable :value="products" scrollable scroll-height="400px">
      <Column field="code" header="Code" />
      <Column field="name" header="Name" />
    </DataTable>
  </div>
</template>
```

## 所有可用预设

| 预设名 | UI 库 | 滚动容器选择器 |
|--------|-------|---------------|
| `'element-plus'` | Element Plus | `.el-scrollbar__wrap` |
| `'ant-design-vue'` | Ant Design Vue | `.ant-table-body` |
| `'naive-ui'` | Naive UI | `.n-scrollbar-container` |
| `'vxe-table'` | VxeTable | `.vxe-table--body-inner-wrapper` |
| `'arco-design'` | Arco Design | `.arco-table-body` |
| `'primevue'` | PrimeVue | `.p-datatable-table-container` |
| `'vuetify'` | Vuetify | `.v-table__wrapper tbody` |

## 没有预设？使用 Selector

对于没有内置预设的组件库，可以通过 `selector` 指定表格内部实际可滚动的容器元素。你需要通过浏览器开发者工具找到该表格组件中负责滚动的 DOM 元素的 CSS 选择器：

```vue
<template>
  <div v-mobile-table="{ selector: '.your-table-scroll-container' }">
    <YourTable :data="data" />
  </div>
</template>
```

::callout{icon="lucide:message-circle-plus" color="blue"}
如果你希望为某个组件库添加内置预设支持，欢迎在 [GitHub](https://github.com/LostElkByte/vue3-mobile-table) 上提交 Issue 或 PR。
::
